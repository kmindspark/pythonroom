# author: kmindspark
n = int (input ("What number would you like to check?"))
tests = range (2, 503)
prime = True

if prime = True:
	for t in tests:
		if n % t == 0:
			print "Composite"
			prime = False
		else:
			print "Prime"
			
print ("Done")